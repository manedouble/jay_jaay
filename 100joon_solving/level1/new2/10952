import java.util.Scanner;
public class Main {
    public static void main(String[] args) {

        Scanner sc = new Scanner (System.in);
        while (true) {
            int a = sc.nextInt();
            int b = sc.nextInt();

            if(a == 0 || b == 0){
                break;
            }
            System.out.println(a + b);
        }
    }
}

// a, b 모두 0이 될 때 끝나는 문제이다. if문을 이용해서 a, b 모두 0일 때 break를 걸어서 탈출하게 만들면 되는 문제
