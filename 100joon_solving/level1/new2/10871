import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        int x = sc.nextInt();

        int[] arr = new int[n];

        for(int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
            if(x > arr[i]) {
                System.out.print(arr[i] + " ");
            }
        }
    }
}

// 나름 간단한 배열, for문으로 n번 반복문 시행을 통해 0번째 index부터 채움, 마지막 공백 입력으로 구분
